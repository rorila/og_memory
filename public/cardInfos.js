// CanrdInfos Klasse
// Diese Klasse enthält alle Informationen zu den Karten, die für das Spiel benötigt werden.
// Die Klasse wird in der Klasse card verwendet.


export class CardInfos {
    card_ID
    constructor(card_ID) {
        this.card_ID = card_ID;
    }
}

