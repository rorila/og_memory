// erzeuge eine Klasse mit Namen: 'Player'. Attribute sind: name, alias und alter und exportiere diese Klasse

export class Player {
    constructor(name, alias, age) {
        this.name = name;
        this.alias = alias;
        this.age = age;
    }
}